import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ejemplosTp from './ejemplosTp'

const index = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  )
}

export default index

const styles = StyleSheet.create({})