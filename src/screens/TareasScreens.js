import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TareasScreens = () => {
  return (
    <View>
      <Text>TareasScreens</Text>
    </View>
  )
}

export default TareasScreens

const styles = StyleSheet.create({})